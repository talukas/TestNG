package core;

import org.testng.annotations.Test;
import static org.testng.Assert.assertEquals;
import org.testng.Assert;
import org.testng.annotations.Test;

public class TestTest {
	@Test(enabled = true)
	public void test() {
	assertEquals("abc", "abc", "Error message");
	} //method (test)
} //class